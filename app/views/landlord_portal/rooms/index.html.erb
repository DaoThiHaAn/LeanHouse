<%# Variables: @house, @rooms, @landlord %>

<% content_for :title, t("page_titles.rooms") %>

<h1 class="w-50 text-center text-clamp-2 m-auto"><%=@house.name%></h1>
<div>

</div>
<p >
    <%=t("form.house.total_floors"): @house.floors_count%>
</p>
</div>