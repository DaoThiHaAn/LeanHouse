<%# Local varianles: unread_count, notifications %>

<% if logged_in? %>
    <div class="dropdown profile-dropdown">
        <button class="py-1 dropdown-toggle d-flex align-items-center gap-2" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <span id="navbar_avatar" class="d-inline-flex align-items-center">
              <%= render "layouts/shared_components/navbar_avatar" %>
            </span>
            <div class="d-flex flex-column align-items-start">
                <p class="fw-bold fullname"><%= format_name(current_user.fullname) %></p>
                <p class="role"><%= get_role %></p>
            </div>
        </button>

        <ul class="dropdown-menu">
          <% if current_user.role == "landlord" %>
            <li><%= link_to t("bar.my_profile"), landlord_profile_path, class: "dropdown-item" %></li>
            <li><%= link_to t("bar.my_bank_acc"), landlord_bank_accounts_path, class: "dropdown-item" %></li>
          <% else %>
            <li><%= link_to t("bar.my_profile"), tenant_profile_path, class: "dropdown-item" %></li>
            <li><%= link_to t("bar.my_vehicle"), tenant_vehicles_path, class: "dropdown-item" %></li>
            <li><%= link_to t("bar.all_contracts"), tenant_all_contracts_path, class: "dropdown-item" %></li>
          <% end %>
          <li>
            <%= link_to logout_path, class: "dropdown-item d-flex gap-1 align-items-center" do %>
              <%= t("auth.log_out") %>
              <span class="material-symbols-outlined">logout</span>
            <% end %>
          </li>
        </ul>
    </div>

    <!-- Mở kết nối WebSocket lắng nghe thông báo riêng của current_user -->
    <%= turbo_stream_from current_user, :notifications %>

    <div id="notification_box" data-controller="notification-sync" data-notification-sync-url-value="<%= box_notifications_path %>">
      <%= render "layouts/shared_components/notification_dropdown",
            unread_count: unread_count,
            notifications: notifications %>
    </div>

<% else %>
    <%= link_to t("auth.sign_up"), signup_path, class: "signup-btn" %>

    <%= link_to login_path, class: "login-btn gap-1 d-flex align-items-center flex-shrink-0" do %>
        <%= t("auth.log_in") %>
        <span class="material-symbols-filled">door_open</span>
    <% end %>
<% end %>
