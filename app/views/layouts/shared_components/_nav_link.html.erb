<div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav me-auto d-flex align-items-center gap-3">
    <%# TODO Authorize and insert the links %>
    <%if !logged_in?%>
        <%= nav_item t("common.bar.home"), "Home", root_path, icon: "home" %>
        <%= nav_item t("common.bar.find_rooms"), "Finding Rooms", posts_path, icon: "search" %>
        <%= nav_item t("common.bar.features"), "Features", features_path%>
        <%= nav_item t("common.bar.about"), "About", about_path %>
        <%= nav_item t("common.bar.contact"), "Contact", contact_path %>

    <%elsif current_user.role == "landlord"%>
        <%= nav_item t("common.bar.dashboard"), "Dashboard", landlord_dashboard_index_path, icon: "insert_chart" %>
        <%= nav_item t("common.bar.house_mng"), "House Management", landlord_houses_path(current_user.id), is_img: true, img_src: "house-mg.png" %>
        <%= nav_item t("common.bar.post_mng"), "Post", landlord_posts_path(current_user.id)%>
        <%= nav_item t("common.bar.request_handle"), "Requests", about_path %>

    <%elsif current_user.role == "tenant"%>
        <%= nav_item t("common.bar.dashboard"), "Dashboard", tenant_dashboard_index_path, icon: "insert_chart" %>
        <%= nav_item t("common.bar.my_room"), "My Room", "#", icon: "location_home" %>
        <%= nav_item t("common.bar.my_contract"), "My Contract", "#"%>
        <%= nav_item t("common.bar.applied_services"), "Services", "#"%>
        <%= nav_item t("common.bar.my_invoices"), "My Invoices", "#" %>
        <%= nav_item t("common.bar.my_requests"), "My Requests", "#" %>
        <%= nav_item t("common.bar.find_rooms"), "Finding Rooms", posts_path, icon: "search" %>
    <%else%>

    <%end%>
    <!-- Dropdown 
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle"
            href="#"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false">
        Dropdown
        </a>

        <ul class="dropdown-menu">
        <li><%= link_to "Action", "#", class: "dropdown-item" %></li>
        <li><%= link_to "Another action", "#", class: "dropdown-item" %></li>
        <li><hr class="dropdown-divider"></li>
        <li><%= link_to "Something else", "#", class: "dropdown-item" %></li>
        </ul>
    </li>
    -->
    </ul>
</div>