<%# Modals for Tenant Portal Invoice Show %>
<%= render "tenant_portal/invoices/zoom_qr_modal",
           invoice: (local_assigns[:invoice] || @invoice),
           bank_account: (local_assigns[:bank_account] || @bank_account) %>

<%= render "landlord_portal/invoices/service_instructions_modal" %>

<%= render "tenant_portal/invoices/invoice_info_modal",
           invoice: (local_assigns[:invoice] || @invoice) %>

<%= render "tenant_portal/invoices/payment_modal",
           invoice: (local_assigns[:invoice] || @invoice),
           modal_id: "tenantMarkPaidModal" %>

<%= render "tenant_portal/invoices/payment_proof_modal",
           invoice: (local_assigns[:invoice] || @invoice) %>
